<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== 权限 ==================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 开发者模式“模拟位置”兜底通道（非 LSPosed 环境用） -->
    <uses-permission
        android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 作用域选择需要枚举已安装应用；模块类应用可申请 -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Android 11+ 包可见性：需要能探测到 LSPosed 管理器，才能在状态面板里
         显示"框架已安装"并一键跳转过去配置作用域 -->
    <queries>
        <package android:name="org.lsposed.manager" />
        <package android:name="org.lsposed.manager.debug" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FakeLoc"
        android:usesCleartextTraffic="true">

        <!-- ==================== Xposed / LSPosed 模块元数据 ==================== -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <!-- 82 = legacy Xposed API 版本，LSPosed 1.9.2 完整兼容 -->
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <!-- 默认作用域：系统框架 + 融合定位服务，用户可在 LSPosed 里增删 -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <!-- 让 LSPosed 额外暴露 XSharedPreferences 通道（配置兜底之一） -->
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <!-- ==================== 入口界面 ==================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== 前台服务（保持模拟位置存活） ==================== -->
        <service
            android:name=".service.FakeLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- ==================== 配置通道 ====================
             hook 代码运行在“目标进程”里（uid 与被 hook 应用相同），
             跨 uid 访问 ContentProvider 必须 exported=true。
             通道里只有坐标/开关等非敏感数据。 -->
        <provider
            android:name=".data.ConfigProvider"
            android:authorities="${applicationId}.config"
            android:enabled="true"
            android:exported="true"
            android:grantUriPermissions="false" />

    </application>

</manifest>
